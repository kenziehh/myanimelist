import { useQuery } from "@tanstack/react-query"

const TopAnime = () => {

  const {}=useQuery({
    queryFn:async()=>{
      const[]
    }
  })
  const animes=await fetch('')
  return (
    <div>
      
    </div>
  )
}

export default TopAnime
